{
  "name": "@asdev/api",
  "private": true,
  "version": "0.0.0",
  "type": "commonjs",
  "scripts": {
    "dev": "bash -lc 'tsc -p tsconfig.json -w --preserveWatchOutput & node --watch dist/server.js'",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "worker:start": "node dist/worker.js",
    "jobs:enqueue:ops": "node dist/jobs-enqueue-ops.js"
  },
  "dependencies": {
    "fastify": "^5.7.4",
    "pg": "^8.19.0"
  },
  "devDependencies": {
    "@types/node": "^25.3.2",
    "@types/pg": "^8.16.0",
    "typescript": "^5.9.3"
  }
}
